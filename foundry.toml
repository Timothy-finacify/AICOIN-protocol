[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]

remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
    "forge-std/=lib/forge-std/src/"
]

solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

via_ir = true
