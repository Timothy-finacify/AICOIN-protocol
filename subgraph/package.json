{
  "name": "aicoin-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ aicoin-protocol",
    "create-local": "graph create --node http://localhost:8020/ aicoin-protocol",
    "remove-local": "graph remove --node http://localhost:8020/ aicoin-protocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aicoin-protocol"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.0",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}